import { ValidateLoginUserPipe } from './validate-login-user.pipe';

describe('ValidateLoginUserPipe', () => {
  it('should be defined', () => {
    expect(new ValidateLoginUserPipe()).toBeDefined();
  });
});
